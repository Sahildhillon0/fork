use crate::token::Token;

pub struct ICG;

impl ICG {
    pub fn new() -> Self {
        ICG
    }

    pub fn generate(&self, tokens: &[Token]) {
        // For demonstration, just print a stub for real intermediate code generation

        for (_i, _token) in tokens.iter().enumerate() {

        }

    }
}
